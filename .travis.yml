language: groovy
sudo: false
cache:
  directories:
  - "$HOME/.gradle/caches"
before_install: echo -e "gradle.publish.key=$APIKEY\ngradle.publish.secret=$APISECRET" >> gradle.properties
script: "travis-ci"
git:
  depth: 5
jdk:
- oraclejdk8
- oraclejdk7
- openjdk7
- openjdk6
