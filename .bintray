{
  "package": {
    "name"": "ensime-gradle",
    "repo": "coacoas",
    "subject": "coacoas"
  }

  "version": {
    "name": "0.2.0-SNAPSHOT"
  }

  "files": [
    {"includePattern": "build/libs/(.*\.jar)", "uploadPattern": "$1"}
  ]
}
